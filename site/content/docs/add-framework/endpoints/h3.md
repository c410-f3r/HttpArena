---
title: HTTP/3
---

Required for the `baseline-h3` and `static-h3` profiles. The same endpoints (`/baseline2` and `/static/*`) are served over **HTTP/3 (QUIC) on port 8443**.

This requires native QUIC support in the framework. Only add H3 tests to your `meta.json` if your framework supports it.
