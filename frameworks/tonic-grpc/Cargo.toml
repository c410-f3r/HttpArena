[package]
name = "tonic-grpc"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.13"
prost = "0.13"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }

[build-dependencies]
tonic-build = "0.13"
