{
  "display_name": "blitz",
  "language": "Zig",
  "type": "framework",
  "engine": "epoll",
  "description": "Zig micro web framework — epoll event loop with pipeline batching, zero-copy parsing, radix-trie router, body discard for uploads, pre-compressed gzip responses, graceful shutdown. Also supports io_uring backend (multishot accept, kernel buffer ring, send_zc) via BLITZ_URING=1. Zero allocations in the hot path.",
  "repo": "https://github.com/BennyFranciscus/blitz",
  "enabled": true,
  "tests": [
    "baseline",
    "pipelined",
    "noisy",
    "limited-conn",
    "json",
    "upload",
    "compression"
  ]
}
