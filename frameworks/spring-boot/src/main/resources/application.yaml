spring:
  application:
    name: spring-boot
  mvc:
    static-path-pattern: /static/**
  web:
    resources:
      static-locations: file:/data/static/
server:
  compression:
    enabled: true
    min-response-size: 500KB
